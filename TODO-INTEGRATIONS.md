# Deferred Integrations

Circle back later when real credentials or cutover configuration are available.

- [ ] Newsletter signup: paste Constant Contact dashboard Universal + Inline embed code into `src/components/home/Newsletter.astro` (`CONSTANT_CONTACT_SIGNUP_EMBED_CODE`); currently a disabled placeholder. Do NOT use the scraped WordPress-plugin form.
- [ ] Donor / donation system: real API keys + endpoint config (TBD which platform).
- [ ] Any other third-party integration secrets to be added at cutover.
